import 'package:flutter/material.dart';

class DijPage extends StatelessWidget {
  const DijPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Página DIJ',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}